---
import Layout from '../layouts/Layout.astro';

const posts = [
  {
    title: "5 razones para usar IA en la gestión de bookmarks",
    excerpt: "Descubre cómo la inteligencia artificial está revolucionando la forma en que organizamos nuestros marcadores web.",
    author: "María González",
    date: "2024-04-15",
    category: "Tecnología",
    image: "🤖"
  },
  {
    title: "Guía definitiva de productividad digital",
    excerpt: "Aprende a optimizar tu flujo de trabajo digital con estas técnicas probadas de gestión de información.",
    author: "Carlos Ruiz",
    date: "2024-04-10",
    category: "Productividad",
    image: "📚"
  },
  {
    title: "El futuro de los navegadores web",
    excerpt: "Un vistazo a las próximas innovaciones que cambiarán la forma en que navegamos por internet.",
    author: "Ana Torres",
    date: "2024-04-05",
    category: "Tendencias",
    image: "🌐"
  }
];
---

<Layout title="Blog - Bookmark Genius">
  <main class="pt-32">
    <section class="py-20">
      <div class="container mx-auto px-4">
        <div class="text-center max-w-3xl mx-auto mb-16">
          <h1 class="text-4xl font-bold mb-6 bg-gradient-to-r from-blue-600 to-indigo-600 text-transparent bg-clip-text">
            Blog
          </h1>
          <p class="text-xl text-gray-600 [text-wrap:balance]">
            Descubre las últimas tendencias en productividad digital y gestión de información.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {posts.map(post => (
            <article class="bg-white rounded-2xl overflow-hidden border border-gray-100 hover:shadow-xl hover:shadow-blue-500/5 transition-all duration-300">
              <div class="aspect-video bg-gradient-to-br from-blue-50 to-indigo-50 flex items-center justify-center">
                <span class="text-6xl">{post.image}</span>
              </div>
              <div class="p-6">
                <div class="flex items-center gap-2 mb-4">
                  <span class="text-sm font-medium text-blue-600 bg-blue-50 px-3 py-1 rounded-full">
                    {post.category}
                  </span>
                  <time class="text-sm text-gray-500" datetime={post.date}>
                    {new Date(post.date).toLocaleDateString('es-ES', {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric'
                    })}
                  </time>
                </div>
                <h2 class="text-xl font-bold mb-2 hover:text-blue-600 transition-colors">
                  <a href={`/blog/${post.title.toLowerCase().replace(/ /g, '-')}`}>
                    {post.title}
                  </a>
                </h2>
                <p class="text-gray-600 mb-4">
                  {post.excerpt}
                </p>
                <div class="flex items-center gap-2">
                  <div class="w-8 h-8 bg-gradient-to-br from-blue-50 to-indigo-50 rounded-full flex items-center justify-center">
                    👤
                  </div>
                  <span class="text-sm text-gray-600">{post.author}</span>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  </main>
</Layout>