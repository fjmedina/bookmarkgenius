---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';

const features = [
  {
    title: "Clasificación Inteligente",
    description: "Nuestro algoritmo de IA analiza el contenido de tus marcadores y los organiza automáticamente en categorías relevantes.",
    icon: "🤖",
    details: [
      "Categorización automática basada en contenido",
      "Aprendizaje continuo de tus preferencias",
      "Etiquetado inteligente"
    ]
  },
  {
    title: "Búsqueda Avanzada",
    description: "Encuentra cualquier marcador al instante con nuestra potente búsqueda semántica y filtros avanzados.",
    icon: "🔍",
    details: [
      "Búsqueda por contenido semántico",
      "Filtros personalizables",
      "Historial de búsqueda"
    ]
  },
  {
    title: "Sincronización",
    description: "Mantén tus marcadores sincronizados en todos tus dispositivos de forma segura y eficiente.",
    icon: "🔄",
    details: [
      "Sincronización en tiempo real",
      "Respaldo automático",
      "Acceso multiplataforma"
    ]
  }
];
---

<Layout title="Funcionalidades - Bookmark Genius">
  <main class="pt-32">
    <section class="py-20">
      <div class="container mx-auto px-4">
        <div class="text-center max-w-3xl mx-auto mb-16">
          <h1 class="text-4xl font-bold mb-6 bg-gradient-to-r from-blue-600 to-indigo-600 text-transparent bg-clip-text">
            Funcionalidades Diseñadas para la Productividad
          </h1>
          <p class="text-xl text-gray-600 [text-wrap:balance]">
            Descubre todas las herramientas que Bookmark Genius pone a tu disposición para mantener tus marcadores perfectamente organizados.
          </p>
        </div>

        <div class="space-y-24">
          {features.map((feature, index) => (
            <div class={`flex flex-col md:flex-row gap-12 items-center ${index % 2 === 1 ? 'md:flex-row-reverse' : ''}`}>
              <div class="flex-1">
                <div class="w-16 h-16 bg-gradient-to-br from-blue-50 to-indigo-50 rounded-2xl flex items-center justify-center mb-6">
                  <span class="text-4xl" role="img" aria-label={feature.title}>{feature.icon}</span>
                </div>
                <h2 class="text-3xl font-bold mb-4 bg-gradient-to-r from-blue-600 to-indigo-600 text-transparent bg-clip-text">
                  {feature.title}
                </h2>
                <p class="text-xl text-gray-600 mb-8">
                  {feature.description}
                </p>
                <ul class="space-y-4">
                  {feature.details.map(detail => (
                    <li class="flex items-center gap-3 text-gray-700">
                      <span class="text-blue-600">✓</span>
                      {detail}
                    </li>
                  ))}
                </ul>
              </div>
              <div class="flex-1 bg-gradient-to-br from-blue-50 to-indigo-50 rounded-3xl p-8 aspect-square flex items-center justify-center">
                <div class="text-8xl">{feature.icon}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section class="py-20 bg-gradient-to-b from-blue-50/50 to-white">
      <div class="container mx-auto px-4 text-center">
        <h2 class="text-3xl font-bold mb-8 bg-gradient-to-r from-blue-600 to-indigo-600 text-transparent bg-clip-text">
          ¿Listo para empezar?
        </h2>
        <Button href="https://chrome.google.com/webstore">
          Descargar Extensión Gratuita
        </Button>
      </div>
    </section>
  </main>
</Layout>